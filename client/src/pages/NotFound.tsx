import PublicLayout from "@/components/PublicLayout";
import { Compass, Mountain, RotateCcw } from "lucide-react";
import { useState } from "react";
import { Link } from "wouter";

const notes = ["The trail bends here. Try a new route.", "A misty turn, not a dead end.", "Your map is fine. This link is not.", "Even locals take a wrong trail sometimes."];
export default function NotFound() { const [note, setNote] = useState(0); return <PublicLayout><main className="mountain-grid grid min-h-[72vh] place-items-center px-4 py-16 text-center"><div><div className="relative mx-auto grid size-24 place-items-center rounded-[2rem] bg-[#123d5b] text-white shadow-2xl"><Mountain className="size-11" /><span className="absolute -right-3 -top-3 rounded-full bg-[#f39a48] px-3 py-1 text-xs font-extrabold text-[#123d5b]">404</span></div><p className="eyebrow mt-8">Lost in the range</p><h1 className="section-title mt-3">This path<br />is not on our map.</h1><p className="mx-auto mt-5 max-w-md text-sm leading-6 text-slate-600">{notes[note]}</p><div className="mt-8 flex flex-wrap justify-center gap-3"><Link href="/" className="focus-ring inline-flex items-center gap-2 rounded-xl bg-[#123d5b] px-5 py-3 text-xs font-extrabold uppercase tracking-[.1em] text-white"><Compass className="size-4" /> Back to base</Link><button onClick={() => setNote(n => (n + 1) % notes.length)} className="focus-ring inline-flex items-center gap-2 rounded-xl bg-[#eef4f2] px-5 py-3 text-xs font-extrabold uppercase tracking-[.1em] text-[#123d5b]"><RotateCcw className="size-4" /> Another trail</button></div></div></main></PublicLayout>; }
