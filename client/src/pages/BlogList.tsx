import PublicLayout from "@/components/PublicLayout";
import { trpc } from "@/lib/trpc";
import { ArrowRight } from "lucide-react";
import { Link } from "wouter";

export default function BlogList() { const { data: blogs = [] } = trpc.blogs.list.useQuery(); return <PublicLayout><section className="bg-[#eaf1ed] py-16"><div className="container"><p className="eyebrow">Field notes</p><h1 className="section-title mt-3">Useful stories<br />from the hills.</h1><p className="mt-5 max-w-lg text-base leading-7 text-slate-600">Planning notes for a more informed Dharamshala and Himachal journey.</p></div></section><section className="container py-14"><div className="grid gap-6 md:grid-cols-2 lg:grid-cols-3">{blogs.map(blog => <Link key={blog.id} href={`/blog/${blog.slug}`} className="group overflow-hidden rounded-[1.35rem] border border-[#dfe8e8] bg-white transition hover:-translate-y-1 hover:shadow-xl"><img src={blog.coverImage} alt="" className="h-48 w-full object-cover transition duration-500 group-hover:scale-105" loading="lazy" /><div className="p-5"><p className="text-xs font-extrabold uppercase tracking-[.1em] text-[#e17818]">{blog.author}</p><h2 className="display mt-3 text-3xl font-bold leading-none text-[#123d5b]">{blog.title}</h2><p className="mt-3 text-sm leading-6 text-slate-600">{blog.excerpt}</p><span className="mt-5 inline-flex items-center gap-2 text-xs font-extrabold uppercase tracking-[.1em] text-[#123d5b]">Read note <ArrowRight className="size-4" /></span></div></Link>)}{!blogs.length && <div className="col-span-full rounded-2xl border border-dashed border-[#ccd9d7] p-10 text-center text-sm text-slate-500">The first field note is on its way. Please check back soon.</div>}</div></section></PublicLayout>; }
