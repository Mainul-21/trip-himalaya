import PublicLayout from "@/components/PublicLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, MessageCircle, Phone } from "lucide-react";
import { FormEvent, useState } from "react";

export default function Contact() { const enquiry = trpc.enquiries.create.useMutation(); const [done, setDone] = useState(false); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); enquiry.mutate({ name: String(data.get("name")), email: String(data.get("email")), phone: String(data.get("phone")) || undefined, subject: String(data.get("subject")), message: String(data.get("message")) }, { onSuccess: () => setDone(true) }); } return <PublicLayout><section className="container grid gap-10 py-14 lg:grid-cols-[.75fr_1.25fr]"><aside className="border border-[#214d68] bg-[#123d5b] p-7 text-white"><p className="display text-3xl font-bold">Talk directly.</p><p className="mt-3 text-sm leading-6 text-white/72">A local planner can help with a route, timing and the right trip for your group.</p><a href="https://wa.me/918609752814?text=Hello%20Trip%20Himalaya%2C%20I%20would%20like%20to%20plan%20a%20trip." target="_blank" rel="noreferrer" className="mt-7 flex items-center gap-3 border border-white/20 bg-white/8 p-4 text-sm font-bold hover:bg-white/15"><MessageCircle className="size-5 text-[#f39a48]" /> Chat on WhatsApp</a><a href="tel:+918609752814" className="mt-3 flex items-center gap-3 border border-white/20 bg-white/8 p-4 text-sm font-bold hover:bg-white/15"><Phone className="size-5 text-[#f39a48]" /> Call our local desk</a></aside>{done ? <div className="grid min-h-80 place-items-center border border-[#cfe6d8] bg-[#f2fbf4] p-8 text-center"><div><CheckCircle2 className="mx-auto size-12 text-[#2d9b61]" /><h2 className="display mt-5 text-4xl font-bold text-[#123d5b]">Message received.</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-slate-600">Thank you for reaching out. We have sent your enquiry to the Trip Himalaya team and will reply with the right next step.</p></div></div> : <form onSubmit={submit} className="border border-[#dfe8e8] bg-white p-6 sm:p-8"><div className="mb-7 border-b border-[#dfe8e8] pb-5"><h2 className="text-xl font-bold text-[#123d5b]">Tell us what you need.</h2><p className="mt-2 text-sm leading-6 text-slate-500">You do not need a final plan. Rough dates and a few details are enough to begin.</p></div><div className="grid gap-5 sm:grid-cols-2"><ContactField name="name" label="Your name" required /><ContactField name="email" label="Email address" type="email" required /><ContactField name="phone" label="Phone / WhatsApp" /><ContactField name="subject" label="What are you planning?" required /><div className="sm:col-span-2"><Label htmlFor="message">Your message</Label><Textarea id="message" name="message" className="mt-2 min-h-32" placeholder="For example: We are two people visiting Dharamshala in October and would like an easy one-day trek." required /></div></div><Button disabled={enquiry.isPending} className="mt-7 h-12 w-full bg-[#e9781c] text-xs font-extrabold uppercase tracking-[.1em] hover:bg-[#d86b12]">{enquiry.isPending ? "Sending…" : "Send enquiry"}</Button></form>}</section></PublicLayout>; }
function ContactField({ name, label, type = "text", required = false }: { name: string; label: string; type?: string; required?: boolean }) { return <div><Label htmlFor={name}>{label}</Label><Input id={name} name={name} type={type} className="mt-2 h-11" required={required} /></div>; }
