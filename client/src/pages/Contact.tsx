import PublicLayout from "@/components/PublicLayout";
import { Button } from "@/components/ui/button";
import { Input } from "@/components/ui/input";
import { Label } from "@/components/ui/label";
import { Textarea } from "@/components/ui/textarea";
import { trpc } from "@/lib/trpc";
import { CheckCircle2, MessageCircle, Phone } from "lucide-react";
import { FormEvent, useState } from "react";

export default function Contact() { const enquiry = trpc.enquiries.create.useMutation(); const [done, setDone] = useState(false); function submit(event: FormEvent<HTMLFormElement>) { event.preventDefault(); const data = new FormData(event.currentTarget); enquiry.mutate({ name: String(data.get("name")), email: String(data.get("email")), phone: String(data.get("phone")) || undefined, subject: String(data.get("subject")), message: String(data.get("message")) }, { onSuccess: () => setDone(true) }); } return <PublicLayout><section className="bg-[#eaf1ed] py-16"><div className="container"><p className="eyebrow">Talk to a local planner</p><h1 className="section-title mt-3">Let’s make it<br />make sense.</h1><p className="mt-5 max-w-xl text-base leading-7 text-slate-600">Give us a few honest details about your time, pace, and people. We will help you find a practical way into the mountains.</p></div></section><section className="container grid gap-10 py-14 lg:grid-cols-[.75fr_1.25fr]"><aside className="rounded-[1.35rem] bg-[#123d5b] p-7 text-white"><p className="display text-3xl font-bold">Reach us your way.</p><p className="mt-3 text-sm leading-6 text-white/70">For quick questions, use WhatsApp or call the Trip Himalaya team. For detailed planning, the form works best.</p><a href="https://wa.me/919999999999?text=Hello%20Trip%20Himalaya%2C%20I%20would%20like%20to%20plan%20a%20trip." target="_blank" rel="noreferrer" className="mt-7 flex items-center gap-3 rounded-xl bg-white/10 p-4 text-sm font-bold hover:bg-white/15"><MessageCircle className="size-5 text-[#f39a48]" /> Chat on WhatsApp</a><a href="tel:+919999999999" className="mt-3 flex items-center gap-3 rounded-xl bg-white/10 p-4 text-sm font-bold hover:bg-white/15"><Phone className="size-5 text-[#f39a48]" /> Call our local desk</a></aside>{done ? <div className="grid min-h-80 place-items-center rounded-[1.35rem] border border-[#cfe6d8] bg-[#f2fbf4] p-8 text-center"><div><CheckCircle2 className="mx-auto size-12 text-[#2d9b61]" /><h2 className="display mt-5 text-4xl font-bold text-[#123d5b]">Message received.</h2><p className="mx-auto mt-3 max-w-md text-sm leading-6 text-slate-600">Thank you for reaching out. We have sent your enquiry to the Trip Himalaya team and will reply with the right next step.</p></div></div> : <form onSubmit={submit} className="rounded-[1.35rem] border border-[#dfe8e8] bg-white p-6 shadow-[0_16px_34px_rgba(18,61,91,.08)] sm:p-8"><div className="grid gap-5 sm:grid-cols-2"><ContactField name="name" label="Your name" required /><ContactField name="email" label="Email address" type="email" required /><ContactField name="phone" label="Phone / WhatsApp" /><ContactField name="subject" label="How can we help?" required /><div className="sm:col-span-2"><Label htmlFor="message">Your message</Label><Textarea id="message" name="message" className="mt-2 min-h-32" placeholder="Tell us the rough dates, group size, and what you would love to experience." required /></div></div><Button disabled={enquiry.isPending} className="mt-7 h-12 w-full rounded-xl bg-[#e9781c] text-xs font-extrabold uppercase tracking-[.1em] hover:bg-[#d86b12]">{enquiry.isPending ? "Sending…" : "Send enquiry"}</Button></form>}</section></PublicLayout>; }
function ContactField({ name, label, type = "text", required = false }: { name: string; label: string; type?: string; required?: boolean }) { return <div><Label htmlFor={name}>{label}</Label><Input id={name} name={name} type={type} className="mt-2 h-11" required={required} /></div>; }
