import PublicLayout, { WhatsAppIcon } from "@/components/PublicLayout";
import { trpc } from "@/lib/trpc";
import { buildTourWhatsAppMessage } from "@/lib/tourWhatsApp";
import { ArrowLeft, CalendarDays, Check, CircleAlert, Clock3, MapPin, Mountain } from "lucide-react";
import { Link, useRoute } from "wouter";

export default function TourDetail() { const [, params] = useRoute("/tours/:slug"); const { data: tour, isLoading } = trpc.tours.bySlug.useQuery({ slug: params?.slug ?? "" }, { enabled: Boolean(params?.slug) }); if (isLoading) return <PublicLayout><div className="container py-32 text-center text-slate-500">Loading your Himalayan journey…</div></PublicLayout>; if (!tour) return <PublicLayout><div className="container py-32"><h1 className="section-title">Journey not found.</h1><Link href="/tours" className="mt-6 inline-flex items-center gap-2 text-sm font-bold text-[#123d5b]"><ArrowLeft className="size-4" /> Back to all journeys</Link></div></PublicLayout>; const whatsAppMessage = buildTourWhatsAppMessage(tour); return <PublicLayout><section className="relative isolate overflow-hidden bg-[#123d5b] text-white"><img src={tour.heroImage} alt={`${tour.title} in ${tour.location}`} className="absolute inset-0 h-full w-full object-cover" /><div className="absolute inset-0 bg-[linear-gradient(90deg,rgba(5,35,55,.9),rgba(5,35,55,.48),rgba(5,35,55,.2))]" /><div className="container relative py-24 sm:py-32"><Link href="/tours" className="focus-ring inline-flex items-center gap-2 rounded-lg text-xs font-bold uppercase tracking-[.1em] text-white/80 hover:text-white"><ArrowLeft className="size-4" /> All journeys</Link><p className="mt-10 text-xs font-extrabold uppercase tracking-[.16em] text-[#f39a48]">{tour.category}</p><h1 className="display mt-3 max-w-3xl text-[clamp(3.2rem,8vw,6rem)] font-bold leading-[.81]">{tour.title}</h1><div className="mt-7 flex flex-wrap gap-x-6 gap-y-3 text-sm text-white/82"><span className="inline-flex items-center gap-2"><MapPin className="size-4 text-[#f39a48]" />{tour.location}</span><span className="inline-flex items-center gap-2"><Clock3 className="size-4 text-[#f39a48]" />{tour.duration}</span><span className="inline-flex items-center gap-2"><Mountain className="size-4 text-[#f39a48]" />{tour.difficulty}</span></div></div></section><section className="container grid gap-12 py-16 lg:grid-cols-[1fr_340px] lg:py-20"><div><p className="text-lg leading-8 text-slate-600">{tour.overview}</p><div className="mt-12"><p className="eyebrow">Good to know</p><h2 className="section-title mt-3">Trip highlights</h2><div className="mt-7 grid gap-3 sm:grid-cols-2">{tour.highlights.map(highlight => <div key={highlight} className="flex gap-3 rounded-xl bg-[#eef4f2] p-4 text-sm font-semibold text-[#214861]"><Check className="mt-0.5 size-4 shrink-0 text-[#e17818]" />{highlight}</div>)}</div></div><div className="mt-14"><p className="eyebrow">A clear rhythm</p><h2 className="section-title mt-3">Day by day</h2><ol className="mt-8 grid gap-4">{tour.itinerary.map(item => <li key={item.day} className="grid gap-3 rounded-2xl border border-[#dfe8e8] bg-white p-5 sm:grid-cols-[90px_1fr]"><span className="display text-2xl font-bold text-[#e17818]">{item.day}</span><div><h3 className="font-bold text-[#123d5b]">{item.title}</h3><p className="mt-2 text-sm leading-6 text-slate-600">{item.description}</p></div></li>)}</ol></div><div className="mt-14 grid gap-6 sm:grid-cols-2"><DetailList title="Included" icon={Check} entries={tour.inclusions} /><DetailList title="Not included" icon={CircleAlert} entries={tour.exclusions} /></div></div><aside className="h-fit rounded-[1.35rem] border border-[#dfe8e8] bg-white p-6 shadow-[0_16px_34px_rgba(18,61,91,.1)] lg:sticky lg:top-24"><p className="text-xs font-extrabold uppercase tracking-[.12em] text-slate-400">From</p><p className="mt-1 text-3xl font-extrabold text-[#123d5b]">₹{tour.priceFrom.toLocaleString("en-IN")} <span className="text-sm font-medium text-slate-400">per person</span></p><p className="mt-5 border-t border-[#edf0ed] pt-5 text-sm leading-6 text-slate-600">Tell us your preferred dates and group size. We will confirm availability before anything is final.</p><Link href={`/book/${tour.slug}`} className="focus-ring mt-6 flex h-12 items-center justify-center gap-2 rounded-xl bg-[#e9781c] text-xs font-extrabold uppercase tracking-[.09em] text-white transition hover:bg-[#d86b12] active:scale-[.97]"><CalendarDays className="size-4" /> Send tour request</Link><a href={`https://wa.me/919999999999?text=${whatsAppMessage}`} target="_blank" rel="noreferrer" className="focus-ring mt-3 flex h-11 items-center justify-center gap-2 rounded-xl bg-[#25d366] text-xs font-extrabold uppercase tracking-[.08em] text-white shadow-[0_8px_18px_rgba(37,211,102,.2)]"><WhatsAppIcon className="size-4" /> Ask about this tour</a></aside></section></PublicLayout>; }
function DetailList({ title, icon: Icon, entries }: { title: string; icon: typeof Check; entries: string[] }) { return <div><h3 className="font-bold text-[#123d5b]">{title}</h3><ul className="mt-4 grid gap-3">{entries.map(entry => <li key={entry} className="flex gap-3 text-sm text-slate-600"><Icon className="mt-0.5 size-4 shrink-0 text-[#e17818]" />{entry}</li>)}</ul></div>; }
